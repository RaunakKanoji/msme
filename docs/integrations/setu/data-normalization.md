# Data normalization

Accounts are processed independently. Mandatory identifiers, decimal money, and UTC timestamps are validated. Invalid transactions are quarantined without discarding valid accounts. Deterministic SHA-256 hashes deduplicate transactions. Narration alone never proves income, debt, fraud, or default.
