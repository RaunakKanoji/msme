# Bridge configuration

Configure branding, `DEPOSIT` FI type, `PROFILE`, `SUMMARY`, and `TRANSACTIONS`, current/savings account discovery, multi-account selection, notification URLs, callback URL, partial fetch, and optional Auto-Fetch. Tags must exist on the product instance before requests use them. Confirm headers and payloads with the current official Setu Postman collection for the configured instance.
