"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import type { Storyline } from "@/lib/submission-storyline";
export function SubmissionStorylineDashboard() { const [items, setItems] = useState<Storyline[]>([]); const [selectedId, setSelectedId] = useState(""); const [state, setState] = useState<"loading" | "ready" | "error">("loading"); const [notice, setNotice] = useState(""); const load = async () => { setState("loading"); try { const response = await fetch("/api/v1/submission-storyline", { headers: { "x-user-role": "bank_analyst" } }); if (!response.ok) throw new Error("Unable to load the submission storyline."); const result: { data: Storyline[] } = await response.json(); setItems(result.data); setSelectedId((value) => value || result.data[0]?.id || ""); setState("ready"); } catch (error) { setNotice(error instanceof Error ? error.message : "Unable to load the submission storyline."); setState("error"); } }; useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer); }, []); const selected = items.find((item) => item.id === selectedId) || items[0]; const record = async () => { if (!selected) return; setNotice("Recording storyline review…"); try { const response = await fetch(`/api/v1/submission-storyline/${selected.id}`, { method: "PATCH", headers: { "content-type": "application/json", "x-user-role": "bank_analyst" }, body: JSON.stringify({ status: selected.status }) }); if (!response.ok) throw new Error("The storyline review could not be recorded."); const result: { data: Storyline; trace_id: string } = await response.json(); setItems((current) => current.map((item) => item.id === result.data.id ? result.data : item)); setNotice(`Storyline review recorded in the immutable audit trail · ${result.trace_id}`); } catch (error) { setNotice(error instanceof Error ? error.message : "The storyline review could not be recorded."); } }; if (state === "loading") return <main className="state-panel" aria-live="polite"><span className="spinner" /> Loading submission storyline…</main>; if (state === "error") return <main className="state-panel error-state" role="alert"><strong>We couldn’t load the submission storyline.</strong><span>{notice}</span><button onClick={() => void load()}>Try again</button></main>; return <main className="workspace"><nav className="workspace-nav" aria-label="Product foundation"><Link href="/app/problem-statement-fit">Problem fit</Link><Link href="/app/target-personas">Personas</Link><Link href="/app/demo-scenarios">Scenarios</Link><Link href="/app/north-star-metrics">Metrics</Link><Link className="active" href="/app/submission-storyline">Storyline</Link></nav><section className="hero" aria-labelledby="story-title"><div><p className="eyebrow">MSME Arogya360 / Product foundation</p><h1 id="story-title">Submission storyline</h1><p>Connect a supportive borrower journey, an evidence-led analyst review, and governed decision support into one credible demo narrative.</p></div><div className="hero-status"><span className="live-dot" /> Narrative registry <small>Synthetic evidence only</small></div></section><section className="metrics" aria-label="Storyline summary"><Metric label="Ready chapters" value={`${items.filter((item) => item.status === "Ready").length}`} note="Presentation-ready" /><Metric label="Average confidence" value={`${Math.round(items.reduce((sum, item) => sum + item.confidence, 0) / Math.max(items.length, 1))}%`} note="Evidence linked" /><Metric label="Stale chapters" value={`${items.filter((item) => item.freshness === "Stale").length}`} note="Refresh before presenting" /></section><section className="content-grid"><div className="queue-card"><div className="section-heading"><div><p className="eyebrow">Narrative registry</p><h2>Submission chapters</h2></div></div><div className="assessment-list">{items.map((item) => <button key={item.id} className={`assessment-row ${selected?.id === item.id ? "selected" : ""}`} onClick={() => setSelectedId(item.id)}><span><strong>{item.chapter}</strong><small>{item.audience} · {item.narrative}</small></span><span className={`status ${item.status === "Ready" ? "ready" : "review"}`}>{item.status}</span><b>{item.confidence}%</b></button>)}</div></div>{selected && <article className="detail-card" aria-live="polite"><div className="detail-top"><div><p className="eyebrow">{selected.audience}</p><h2>{selected.chapter}</h2></div><span className={`status ${selected.status === "Ready" ? "ready" : "review"}`}>{selected.status}</span></div><div className="confidence"><span>Narrative confidence</span><strong>{selected.confidence}%</strong><div><i style={{ width: `${selected.confidence}%` }} /></div></div><div className="recommendation"><span>Presentation guidance</span><strong>{selected.recommendation}</strong><p>{selected.explanation}</p></div><h3>Narrative evidence</h3><div className="evidence-list"><div className="evidence"><div><strong>{selected.narrative}</strong><p>Source {selected.source_id}; raw sensitive payloads and production claims are excluded.</p><small>Observed {new Date(selected.source_observed_at).toLocaleDateString("en-IN", { day: "2-digit", month: "short", year: "numeric" })}</small></div><div><span className={`freshness ${selected.freshness === "Current" ? "current" : "stale"}`}>{selected.freshness}</span><b>{selected.completeness}% complete</b></div></div></div><button className="primary-action" onClick={() => void record()}>Record storyline review</button>{notice && <p className="notice" role="status">{notice}</p>}</article>}</section></main>; }
function Metric({ label, value, note }: { label: string; value: string; note: string }) { return <div className="metric"><span>{label}</span><strong>{value}</strong><small>{note}</small></div>; }
