"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export default function SetuConsentForm() { const router = useRouter(); const [mobile, setMobile] = useState(""); const [periodic, setPeriodic] = useState(false); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const end = new Date(); const start = new Date(end); start.setFullYear(start.getFullYear() - 1); async function submit() { setBusy(true); setError(""); try { const response = await fetch("/api/integrations/setu/consents", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ mobileNumber: mobile, from: start.toISOString(), to: end.toISOString(), fetchType: periodic ? "PERIODIC" : "ONETIME" }) }); const body = await response.json(); if (!response.ok) throw new Error(body.title); window.location.assign(body.data.approvalUrl); } catch (value) { setError(value instanceof Error ? value.message : "Consent could not be started."); setBusy(false); } }
return <main className="workspace"><nav className="workspace-nav"><a href="/app/data-connections">Data connections</a><a className="active" href="/app/data-connections/setu">Setu AA</a></nav><section className="hero"><div><p className="eyebrow">Before you continue</p><h1>Connect business bank accounts</h1><p>Review exactly how your information will be used before leaving for the Setu-hosted consent journey.</p></div></section><section className="content-grid"><article className="detail-card"><div className="detail-top"><div><p className="eyebrow">Requested information</p><h2>12 months of deposit-account data</h2></div></div><ul><li>Account profile, summary, and transactions</li><li>Current accounts and business-use savings accounts</li><li>Multiple account selection is supported</li><li>Processed for 30 days in the initial configuration</li></ul><p>We use your consented bank-account information to calculate cash-flow stability, financial-health indicators, business-risk signals and an explainable 12-month default-risk estimate.</p><p>Access does not guarantee approval or eligibility, cannot transfer money, and never shares bank passwords with MSME Arogya360.</p></article><article className="detail-card"><div className="detail-top"><div><p className="eyebrow">Consent choices</p><h2>Confirm your request</h2></div></div><label className="ui-label" htmlFor="setu-mobile">Mobile number registered with your bank</label><input className="ui-input" id="setu-mobile" inputMode="numeric" pattern="[0-9]{10}" maxLength={10} value={mobile} onChange={(event) => setMobile(event.target.value.replace(/\D/g, ""))} /><label className="check-label"><input type="checkbox" checked={periodic} onChange={(event) => setPeriodic(event.target.checked)} />Enable monthly monitoring; leave off for the default one-time consent.</label>{error && <p className="address-missing">{error}</p>}<div className="button-row"><button className="ui-button" disabled={busy || mobile.length !== 10} onClick={submit}>{busy ? "Creating consent…" : "Continue to Setu"}</button><button className="ui-button ui-button-outline" onClick={() => router.push("/app/data-connections")}>Cancel</button></div><small>You can revoke consent later from Settings. Sandbox mode is clearly identified after the connection starts.</small></article></section></main>; }
