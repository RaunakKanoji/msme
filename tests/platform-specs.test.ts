import assert from "node:assert/strict"; import test from "node:test";
// Uniform coverage for batches 10-21 platform-spec slices (generated per spec on the shared factory).
const SLUGS = ["owner-dashboard", "health-card-explainer", "risk-outlook-explainer", "improvement-plan", "invoice-collections-actions", "cash-buffer-coaching", "document-upload-center", "consent-management", "support-request", "multilingual-notifications", "admin-dashboard", "user-management", "role-permission-management", "model-registry-admin", "score-configuration", "policy-version-management", "feature-flag-management", "audit-event-viewer", "tenant-settings", "retention-configuration", "borrower-health-card-pdf", "analyst-risk-report", "credit-decision-memo", "model-explainability-report", "compliance-evidence-report", "portfolio-risk-report", "branch-risk-report", "data-quality-report", "export-permission-controls", "download-center", "idbi-sandbox-gateway", "synthetic-dataset-loader", "sandbox-authentication", "api-throttling-retries", "webhook-receiver", "sandbox-secrets-management", "fallback-demo-mode", "integration-test-harness", "mock-server", "postman-openapi-collection", "design-tokens", "color-palette", "typography", "layout-grid", "sidebar-navigation-design", "dashboard-cards", "risk-badges", "charts-and-visualizations", "forms-and-validation", "motion-interactions", "dark-mode", "responsive-behaviour", "threat-model", "dpdp-privacy-controls", "rbi-digital-lending-controls", "consent-evidence-policy", "data-localization", "encryption-key-management", "secrets-management", "access-control-matrix", "audit-log-policy", "incident-response", "vulnerability-management", "data-minimization", "api-overview", "auth-api", "businesses-api", "applications-api", "consents-api", "connectors-api", "health-card-api", "predictions-api", "decisioning-api", "alerts-api", "reports-api", "admin-api", "webhooks-api", "error-taxonomy", "erd-overview", "organization-tables", "user-role-tables", "business-application-tables", "consent-tables", "source-data-tables", "canonical-data-tables", "feature-snapshot-tables", "health-card-tables", "prediction-tables", "decision-audit-tables", "report-tables", "migrations-strategy", "seed-data-strategy", "ml-service-structure", "training-pipeline", "feature-store-strategy", "model-registry", "experiment-tracking", "model-card-template", "champion-challenger", "bias-fairness-checks", "drift-monitoring", "model-api-contract", "model-test-suite", "model-rollback", "environment-strategy", "ci-cd-pipeline", "docker-compose-local", "aws-reference-architecture", "neon-postgres-setup", "redis-queue-setup", "object-storage-setup", "logging-standard", "metrics-standard", "distributed-tracing", "alerting-rules", "backup-restore-runbook", "unit-test-plan", "integration-test-plan", "e2e-test-plan", "api-contract-test-plan", "data-quality-test-plan", "model-validation-test-plan", "security-test-plan", "accessibility-test-plan", "performance-test-plan", "uat-plan", "demo-readiness-checklist", "release-gate-checklist", "support-dashboard", "rm-task-queue", "borrower-service-requests", "sla-policy", "ops-runbook", "connector-failure-runbook", "prediction-failure-runbook", "audit-support-runbook", "compliance-review-runbook", "post-incident-review"];
const analyst = { "x-user-role": "bank_analyst", "content-type": "application/json" };
for (const slug of SLUGS) {
  test(`platform ${slug}: CRUD, RBAC, audit`, async () => {
    const { handlers, store } = (await import(`../lib/platform-specs/${slug}.ts`)) as { handlers: { GET: (r: Request) => Promise<Response>; POST: (r: Request) => Promise<Response>; PATCH: (r: Request, c: { params: Promise<{ id: string }> }) => Promise<Response> }; store: { auditCount: () => number } };
    const ok = await handlers.GET(new Request("http://t", { headers: analyst }));
    const body = await ok.json();
    assert.equal(ok.status, 200); assert.equal(body.data.records.length, 2); assert.ok(body.trace_id.startsWith("trc_"));
    const denied = await handlers.GET(new Request("http://t", { headers: { "x-user-role": "borrower" } }));
    assert.equal(denied.status, 403);
    const invalid = await handlers.POST(new Request("http://t", { method: "POST", headers: analyst, body: "{}" }));
    assert.equal(invalid.status, 400);
    const before = store.auditCount();
    const created = await handlers.POST(new Request("http://t", { method: "POST", headers: analyst, body: JSON.stringify({ label: "T", version: "t-v1", summary: "S" }) }));
    const cb = await created.json();
    assert.equal(created.status, 201);
    const patched = await handlers.PATCH(new Request("http://t", { method: "PATCH", headers: analyst, body: JSON.stringify({ status: "Adopted" }) }), { params: Promise.resolve({ id: cb.data.id }) });
    assert.equal(patched.status, 200);
    assert.equal(store.auditCount(), before + 2);
  });
}
