import assert from "node:assert/strict"; import test from "node:test"; import { GET, POST } from "../app/api/v1/customer-concentration-score/route.ts"; import { PATCH } from "../app/api/v1/customer-concentration-score/[id]/route.ts"; import { scoreAuditCount } from "../lib/customer-concentration-score.ts";
const headers = { "x-user-role": "bank_analyst" };
test("returns the seeded customer concentration score with a trace id", async () => { const response = await GET(new Request("http://test/api/v1/customer-concentration-score", { headers })); const payload = await response.json(); assert.equal(response.status, 200); assert.equal(payload.data.scores[0].masked_reference, "CC-••••-9945"); assert.ok(payload.trace_id.startsWith("trc_")); });
test("rejects incomplete customer concentration score creation", async () => { const response = await POST(new Request("http://test/api/v1/customer-concentration-score", { method: "POST", headers: { ...headers, "content-type": "application/json" }, body: "{}" })); const payload = await response.json(); assert.equal(response.status, 400); assert.equal(payload.error.code, "VALIDATION_ERROR"); });
test("denies unauthorized customer concentration score access", async () => { const response = await GET(new Request("http://test/api/v1/customer-concentration-score", { headers: { "x-user-role": "borrower" } })); const payload = await response.json(); assert.equal(response.status, 403); assert.equal(payload.error.code, "AUTHORIZATION_DENIED"); assert.equal("data" in payload, false); });
test("audits customer concentration score changes", async () => { const before = scoreAuditCount(); const response = await PATCH(new Request("http://test/api/v1/customer-concentration-score/customer_concentration_score_demo_002", { method: "PATCH", headers: { ...headers, "content-type": "application/json" }, body: JSON.stringify({ status: "Published" }) }), { params: Promise.resolve({ id: "customer_concentration_score_demo_002" }) }); assert.equal(response.status, 200); assert.equal(scoreAuditCount(), before + 1); });
