import assert from "node:assert/strict"; import test from "node:test"; import { GET, POST } from "../app/api/v1/borrower-health-card-view/route.ts"; import { PATCH } from "../app/api/v1/borrower-health-card-view/[id]/route.ts"; import { borrowerHealthCardViewAuditCount } from "../lib/borrower-health-card-view.ts";
const headers = { "x-user-role": "bank_analyst" };
test("returns the seeded borrower health card view with a trace id", async () => { const response = await GET(new Request("http://test/api/v1/borrower-health-card-view", { headers })); const payload = await response.json(); assert.equal(response.status, 200); assert.equal(payload.data.views[0].masked_reference, "BHC-••••-3020"); assert.ok(payload.trace_id.startsWith("trc_")); });
test("rejects incomplete borrower health card view creation", async () => { const response = await POST(new Request("http://test/api/v1/borrower-health-card-view", { method: "POST", headers: { ...headers, "content-type": "application/json" }, body: "{}" })); const payload = await response.json(); assert.equal(response.status, 400); assert.equal(payload.error.code, "VALIDATION_ERROR"); });
test("denies unauthorized borrower health card view access", async () => { const response = await GET(new Request("http://test/api/v1/borrower-health-card-view", { headers: { "x-user-role": "borrower" } })); const payload = await response.json(); assert.equal(response.status, 403); assert.equal(payload.error.code, "AUTHORIZATION_DENIED"); assert.equal("data" in payload, false); });
test("audits borrower health card view changes", async () => { const before = borrowerHealthCardViewAuditCount(); const response = await PATCH(new Request("http://test/api/v1/borrower-health-card-view/borrower_view_demo_002", { method: "PATCH", headers: { ...headers, "content-type": "application/json" }, body: JSON.stringify({ status: "Published" }) }), { params: Promise.resolve({ id: "borrower_view_demo_002" }) }); assert.equal(response.status, 200); assert.equal(borrowerHealthCardViewAuditCount(), before + 1); });
