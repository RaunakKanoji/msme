"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function SignInPage() { const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setLoading(true); setMessage(""); try { const response = await fetch("/api/v1/signin-flow", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email, password }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error?.message || "Sign-in is unavailable."); router.push(payload.data.redirect); } catch (error) { setMessage(error instanceof Error ? error.message : "Sign-in is unavailable."); setLoading(false); } }; return <main className="auth-page"><section><p className="eyebrow">MSME Arogya360</p><h1>Welcome back</h1><p>Sign in to continue your evidence-led journey.</p><form onSubmit={submit}><label>Work email<input value={email} onChange={(event) => setEmail(event.target.value)} type="email" placeholder="name@organisation.com" required /></label><label>Password<input value={password} onChange={(event) => setPassword(event.target.value)} type="password" placeholder="Minimum 8 characters" minLength={8} required /></label><button className="landing-cta" disabled={loading}>{loading ? "Signing in…" : "Sign in to demo"}</button>{message && <p role="alert">{message}</p>}</form><small>Synthetic demo session only. No production credentials are used.</small><Link href="/signup">New to MSME Arogya360? Create an account</Link></section></main>; }
