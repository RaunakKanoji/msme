"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function SignUpPage() { const router = useRouter(); const [email, setEmail] = useState(""); const [businessName, setBusinessName] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setLoading(true); setMessage(""); try { const response = await fetch("/api/v1/signup-invite-flow", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email, business_name: businessName }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error?.message || "Signup is unavailable."); router.push(payload.data.redirect); } catch (error) { setMessage(error instanceof Error ? error.message : "Signup is unavailable."); setLoading(false); } }; return <main className="auth-page"><section><p className="eyebrow">MSME Arogya360</p><h1>Start your readiness journey</h1><p>We’ll guide you through the evidence you can choose to share.</p><form onSubmit={submit}><label>Business email<input value={email} onChange={(event) => setEmail(event.target.value)} type="email" placeholder="owner@business.in" required /></label><label>Business name<input value={businessName} onChange={(event) => setBusinessName(event.target.value)} type="text" placeholder="Your MSME name" minLength={2} required /></label><button className="landing-cta" disabled={loading}>{loading ? "Creating invite…" : "Create demo workspace"}</button>{message && <p role="alert">{message}</p>}</form><small>Synthetic demo invitation only. No real invite is sent.</small><Link href="/signin">Already have an account? Sign in</Link></section></main>; }
