import { apiError, createGstConnector, getActor, getGstConnectors, isAllowedRole, traceId } from "../../../../lib/gst-connector.ts";
export async function GET(request: Request) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to view GST connectors.", 403); return Response.json({ data: getGstConnectors(), trace_id }); }
export async function POST(request: Request) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to create a GST connector.", 403); let body: Record<string, unknown>; try { body = await request.json(); } catch { return apiError(trace_id, "VALIDATION_ERROR", "Request body must be valid JSON.", 400); } const fields = ["business_name", "purpose", "masked_gstin"] as const; if (fields.some((field) => typeof body[field] !== "string" || !body[field].trim())) return apiError(trace_id, "VALIDATION_ERROR", "business_name, purpose, and masked_gstin are required.", 400); return Response.json({ data: createGstConnector(body as { business_name: string; purpose: string; masked_gstin: string }, actor.id), trace_id }, { status: 201 }); }
