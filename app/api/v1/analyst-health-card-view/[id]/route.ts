import { ANALYST_VIEW_STATUSES, apiError, getActor, getAnalystHealthCardView, isAllowedRole, traceId, updateAnalystHealthCardView } from "../../../../../lib/analyst-health-card-view.ts";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to update the analyst health card view.", 403); const { id } = await params; if (!getAnalystHealthCardView(id)) return apiError(trace_id, "NOT_FOUND", "Analyst health card view was not found.", 404); let body: Record<string, unknown>; try { body = await request.json(); } catch { return apiError(trace_id, "VALIDATION_ERROR", "Request body must be valid JSON.", 400); } if (!ANALYST_VIEW_STATUSES.includes(body.status as never)) return apiError(trace_id, "VALIDATION_ERROR", "status must be a supported analyst view status.", 400); return Response.json({ data: updateAnalystHealthCardView(id, body.status as "Published" | "Needs review" | "Draft", actor.id), trace_id }); }
