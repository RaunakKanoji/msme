import { apiError, createUliOcenAdapter, getActor, getUliOcenAdapters, isAllowedRole, traceId } from "../../../../lib/uli-ocen-adapter.ts";
export async function GET(request: Request) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to view ULI/OCEN adapters.", 403); return Response.json({ data: getUliOcenAdapters(), trace_id }); }
export async function POST(request: Request) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to create a ULI/OCEN adapter.", 403); let body: Record<string, unknown>; try { body = await request.json(); } catch { return apiError(trace_id, "VALIDATION_ERROR", "Request body must be valid JSON.", 400); } const fields = ["business_name", "purpose", "masked_request_id"] as const; if (fields.some((field) => typeof body[field] !== "string" || !body[field].trim())) return apiError(trace_id, "VALIDATION_ERROR", "business_name, purpose, and masked_request_id are required.", 400); return Response.json({ data: createUliOcenAdapter(body as { business_name: string; purpose: string; masked_request_id: string }, actor.id), trace_id }, { status: 201 }); }
