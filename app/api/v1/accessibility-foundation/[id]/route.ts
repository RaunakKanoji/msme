import { apiError, getActor, getPolicy, isAllowedRole, traceId } from "../../../../../lib/accessibility-foundation.ts";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to configure accessibility policies.", 403); const { id } = await params; return getPolicy(id) ? Response.json({ data: getPolicy(id), trace_id }) : apiError(trace_id, "NOT_FOUND", "Accessibility policy was not found.", 404); }
