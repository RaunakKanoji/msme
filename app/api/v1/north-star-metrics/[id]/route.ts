import { apiError, getActor, getMetric, isAllowedRole, traceId, updateMetric } from "../../../../../lib/north-star-metrics.ts";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to configure north star metrics.", 403); const { id } = await params; if (!getMetric(id)) return apiError(trace_id, "NOT_FOUND", "Metric was not found.", 404); let body: Record<string, unknown>; try { body = await request.json(); } catch { return apiError(trace_id, "VALIDATION_ERROR", "Request body must be valid JSON.", 400); } const fields = ["name", "value", "target", "explanation", "status"]; const changes = Object.fromEntries(Object.entries(body).filter(([key, value]) => fields.includes(key) && typeof value === "string" && value.trim())); if (!Object.keys(changes).length) return apiError(trace_id, "VALIDATION_ERROR", "Provide at least one valid field to update.", 400); return Response.json({ data: updateMetric(id, changes, actor.id), trace_id }); }
