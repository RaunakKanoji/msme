import { apiError, createMetric, getActor, isAllowedRole, listMetrics, traceId } from "../../../../lib/north-star-metrics.ts";
export async function GET(request: Request) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to view north star metrics.", 403); return Response.json({ data: listMetrics(), trace_id }); }
export async function POST(request: Request) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to configure north star metrics.", 403); let body: Record<string, unknown>; try { body = await request.json(); } catch { return apiError(trace_id, "VALIDATION_ERROR", "Request body must be valid JSON.", 400); } if (!(["Track 03", "Track 04", "Shared"].includes(body.track as string)) || ["name", "value", "target", "explanation"].some((key) => typeof body[key] !== "string" || !body[key].trim())) return apiError(trace_id, "VALIDATION_ERROR", "name, track, value, target, and explanation are required.", 400); return Response.json({ data: createMetric(body as { name: string; track: "Track 03" | "Track 04" | "Shared"; value: string; target: string; explanation: string }, actor.id), trace_id }, { status: 201 }); }
