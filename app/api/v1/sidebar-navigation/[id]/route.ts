import { apiError, getActor, getNavigationItem, isAllowedRole, traceId } from "../../../../../lib/sidebar-navigation.ts";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to configure navigation.", 403); const { id } = await params; return getNavigationItem(id) ? Response.json({ data: getNavigationItem(id), trace_id }) : apiError(trace_id, "NOT_FOUND", "Navigation item was not found.", 404); }
