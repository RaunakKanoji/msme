import { apiError, getActor, getHealthCard, HEALTH_CARD_STATUSES, isAllowedRole, traceId, updateHealthCard } from "../../../../../lib/health-score-framework.ts";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const trace_id = traceId(); const actor = getActor(request); if (!isAllowedRole(actor.role)) return apiError(trace_id, "AUTHORIZATION_DENIED", "You are not authorized to update a health card.", 403); const { id } = await params; if (!getHealthCard(id)) return apiError(trace_id, "NOT_FOUND", "Health card was not found.", 404); let body: Record<string, unknown>; try { body = await request.json(); } catch { return apiError(trace_id, "VALIDATION_ERROR", "Request body must be valid JSON.", 400); } if (!HEALTH_CARD_STATUSES.includes(body.status as never)) return apiError(trace_id, "VALIDATION_ERROR", "status must be a supported health card status.", 400); return Response.json({ data: updateHealthCard(id, body.status as "Published" | "Needs review" | "Draft", actor.id), trace_id }); }
