import { apiError, getActor, isAllowedRole, traceId } from "./problem-statement-fit.ts";
export { apiError, getActor, isAllowedRole, traceId };
export type LandingConfiguration = { id: string; headline: string; status: "Published" | "Review required"; value_proposition: string; explanation: string; source_id: string; source_observed_at: string; freshness: "Current" | "Stale"; completeness: number; created_at: string; updated_at: string; created_by: string; updated_by: string; };
let configurations: LandingConfiguration[] = [{ id: "landing_default_001", headline: "Credit readiness, made clearer for every MSME.", status: "Published", value_proposition: "MSME Arogya360 brings permitted business evidence, explainable financial health insights, and assisted bank review into one supportive journey.", explanation: "Synthetic demo mode keeps the experience useful before sandbox access, without claiming production connectivity or autonomous credit approval.", source_id: "landing_content_v1", source_observed_at: "2026-07-12T08:00:00.000Z", freshness: "Current", completeness: 100, created_at: "2026-07-12T08:00:00.000Z", updated_at: "2026-07-12T08:00:00.000Z", created_by: "system.seed", updated_by: "system.seed" }]; const audits: Array<Record<string, string>> = [];
export function listLandingConfigurations() { return configurations; } export function getLandingConfiguration(id: string) { return configurations.find((entry) => entry.id === id); }
export function createLandingConfiguration(input: Pick<LandingConfiguration, "headline" | "value_proposition" | "explanation">, actor: string) { const now = new Date().toISOString(); const entry: LandingConfiguration = { id: `landing_${crypto.randomUUID()}`, ...input, status: "Review required", source_id: "manual_landing_entry", source_observed_at: now, freshness: "Current", completeness: 0, created_at: now, updated_at: now, created_by: actor, updated_by: actor }; configurations = [entry, ...configurations]; audit("landing_page.created", entry.id, actor); return entry; }
export function updateLandingConfiguration(id: string, input: Partial<Pick<LandingConfiguration, "headline" | "value_proposition" | "explanation" | "status">>, actor: string) { const current = getLandingConfiguration(id); if (!current) return undefined; const updated = { ...current, ...input, updated_at: new Date().toISOString(), updated_by: actor }; configurations = configurations.map((entry) => entry.id === id ? updated : entry); audit("landing_page.updated", id, actor); return updated; }
function audit(event_type: string, entity_id: string, actor_id: string) { audits.push({ id: `audit_${crypto.randomUUID()}`, event_type, entity_id, actor_id, occurred_at: new Date().toISOString() }); } export function landingAuditEventCount() { return audits.length; }
