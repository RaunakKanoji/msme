import { apiError, getActor, isAllowedRole, traceId } from "./problem-statement-fit.ts";
export { apiError, getActor, isAllowedRole, traceId };
export type ShellConfiguration = { id: string; workspace_name: string; status: "Active" | "Review required"; navigation: string[]; explanation: string; source_id: string; source_observed_at: string; freshness: "Current" | "Stale"; completeness: number; created_at: string; updated_at: string; created_by: string; updated_by: string; };
let configurations: ShellConfiguration[] = [{ id: "shell_default_001", workspace_name: "MSME Arogya360 analyst workspace", status: "Active", navigation: ["Overview", "Problem fit", "Track scope", "Demo scenarios", "Governance"], explanation: "The shell provides role-aware navigation and operational context using synthetic demo state. Connector or ML dependency failures do not block the rest of the workspace.", source_id: "app_shell_config_v1", source_observed_at: "2026-07-12T08:00:00.000Z", freshness: "Current", completeness: 100, created_at: "2026-07-12T08:00:00.000Z", updated_at: "2026-07-12T08:00:00.000Z", created_by: "system.seed", updated_by: "system.seed" }]; const audits: Array<Record<string, string>> = [];
export function listShellConfigurations() { return configurations; } export function getShellConfiguration(id: string) { return configurations.find((item) => item.id === id); }
export function createShellConfiguration(input: Pick<ShellConfiguration, "workspace_name" | "explanation">, actor: string) { const now = new Date().toISOString(); const item: ShellConfiguration = { id: `shell_${crypto.randomUUID()}`, ...input, status: "Review required", navigation: [], source_id: "manual_shell_entry", source_observed_at: now, freshness: "Current", completeness: 0, created_at: now, updated_at: now, created_by: actor, updated_by: actor }; configurations = [item, ...configurations]; audit("bank_grade_app_shell.created", item.id, actor); return item; }
export function updateShellConfiguration(id: string, input: Partial<Pick<ShellConfiguration, "workspace_name" | "explanation" | "status">>, actor: string) { const current = getShellConfiguration(id); if (!current) return undefined; const updated = { ...current, ...input, updated_at: new Date().toISOString(), updated_by: actor }; configurations = configurations.map((item) => item.id === id ? updated : item); audit("bank_grade_app_shell.updated", id, actor); return updated; }
function audit(event_type: string, entity_id: string, actor_id: string) { audits.push({ id: `audit_${crypto.randomUUID()}`, event_type, entity_id, actor_id, occurred_at: new Date().toISOString() }); } export function shellAuditEventCount() { return audits.length; }
