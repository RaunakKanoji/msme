import { apiError, getActor, isAllowedRole, traceId } from "./problem-statement-fit.ts";
export { apiError, getActor, isAllowedRole, traceId };
export type SessionPolicy = { id: string; name: string; status: "Active" | "Review required"; timeout: string; explanation: string; source_id: string; source_observed_at: string; freshness: "Current" | "Stale"; completeness: number; created_at: string; updated_at: string; created_by: string; updated_by: string; };
let policies: SessionPolicy[] = [{ id: "session_policy_001", name: "Clerk-managed authenticated session", status: "Active", timeout: "Managed by Clerk", explanation: "Clerk protects non-public routes. The workspace exposes a visible user menu so users can recognize and manage their active account.", source_id: "session_policy_v1", source_observed_at: "2026-07-12T08:00:00.000Z", freshness: "Current", completeness: 100, created_at: "2026-07-12T08:00:00.000Z", updated_at: "2026-07-12T08:00:00.000Z", created_by: "system.seed", updated_by: "system.seed" }]; const audits: Array<Record<string, string>> = [];
export function listPolicies() { return policies; } export function getPolicy(id: string) { return policies.find((policy) => policy.id === id); } export function recordSessionAction(id: string, actor: string) { const policy = getPolicy(id); if (!policy) return undefined; audit("session_management.reviewed", id, actor); return policy; } export function createPolicy(input: Pick<SessionPolicy, "name" | "timeout" | "explanation">, actor: string) { const now = new Date().toISOString(); const policy: SessionPolicy = { id: `session_${crypto.randomUUID()}`, ...input, status: "Review required", source_id: "manual_session_policy", source_observed_at: now, freshness: "Current", completeness: 0, created_at: now, updated_at: now, created_by: actor, updated_by: actor }; policies = [policy, ...policies]; audit("session_management.created", policy.id, actor); return policy; } export function sessionAuditCount() { return audits.length; } function audit(event_type: string, entity_id: string, actor_id: string) { audits.push({ id: `audit_${crypto.randomUUID()}`, event_type, entity_id, actor_id, occurred_at: new Date().toISOString() }); }
