import { apiError, getActor, isAllowedRole, traceId } from "./problem-statement-fit.ts";
export { apiError, getActor, isAllowedRole, traceId };
export type InviteConfiguration = { id: string; provider: string; status: "Active" | "Review required"; explanation: string; source_id: string; source_observed_at: string; freshness: "Current" | "Stale"; completeness: number; created_at: string; updated_at: string; created_by: string; updated_by: string; };
let configurations: InviteConfiguration[] = [{ id: "invite_demo_001", provider: "Synthetic demo invite", status: "Active", explanation: "Demo registration validates business details locally and creates a synthetic workspace invitation; no real invitation is delivered.", source_id: "invite_demo_config_v1", source_observed_at: "2026-07-12T08:00:00.000Z", freshness: "Current", completeness: 100, created_at: "2026-07-12T08:00:00.000Z", updated_at: "2026-07-12T08:00:00.000Z", created_by: "system.seed", updated_by: "system.seed" }]; const audits: Array<Record<string, string>> = [];
export function listInviteConfigurations() { return configurations; } export function getInviteConfiguration(id: string) { return configurations.find((item) => item.id === id); }
export function createInviteConfiguration(input: Pick<InviteConfiguration, "provider" | "explanation">, actor: string) { const now = new Date().toISOString(); const item: InviteConfiguration = { id: `invite_${crypto.randomUUID()}`, ...input, status: "Review required", source_id: "manual_invite_entry", source_observed_at: now, freshness: "Current", completeness: 0, created_at: now, updated_at: now, created_by: actor, updated_by: actor }; configurations = [item, ...configurations]; audit("signup_invite_flow.created", item.id, actor); return item; }
export function updateInviteConfiguration(id: string, input: Partial<Pick<InviteConfiguration, "provider" | "explanation" | "status">>, actor: string) { const current = getInviteConfiguration(id); if (!current) return undefined; const updated = { ...current, ...input, updated_at: new Date().toISOString(), updated_by: actor }; configurations = configurations.map((item) => item.id === id ? updated : item); audit("signup_invite_flow.updated", id, actor); return updated; }
export function recordSignup(email: string) { audit("signup_invite_flow.workspace_requested", "invite_demo_001", email.replace(/(.{2}).+(@.*)/, "$1***$2")); } function audit(event_type: string, entity_id: string, actor_id: string) { audits.push({ id: `audit_${crypto.randomUUID()}`, event_type, entity_id, actor_id, occurred_at: new Date().toISOString() }); } export function inviteAuditEventCount() { return audits.length; }
